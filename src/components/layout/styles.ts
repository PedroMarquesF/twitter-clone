import styled from 'styled-components';

export const Container = styled.div`background: var(--primary);`;
export const Wrapper = styled.div`
	height: auto;
	max-width: 1280px;
	margin: 0 auto;

	display: flex;
	justify-content: center;
`;
